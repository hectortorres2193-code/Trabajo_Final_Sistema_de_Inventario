//aqui pondremos las liberias nesesarias
#include <stdio.h>
void menu();//funcion 1
void guardardatos();//funcion 2
int main()
{
printf("Registro de ventas\n");
//poner todas las funciones aqui antes
FILE *clientes= fopen("clientes.txt", "r");
FILE *Productos= fopen("Productos.txt", "r");
FILE *Fechas= fopen("Fechas.txt", "r");
FILE *Facturacion;
FILE *central   = fopen("central.txt", "w");
fprintf(central, " ");
copiarContenido(clientes, central);
fprintf(central, " ");
copiarContenido(Productos, central);
fprintf(central, " ");
copiarContenido(Fechas, central);
    fclose(clientes);
    fclose(Productos);
    fclose(Fechas);
    fclose(central);

    return 0;
}
void menu() {//funcion 1
    printf("1.- Lista de clientes.\n"
           "2.- Lista de productos.\n"
           "3.- Precios.\n"
           "4.- Ver Facturacion por fechas.\n"
           "5.- Salir.\n");
}
void guardardatos(){
    FILE *clientes = fopen("registro.txt", "a");
        printf("Nombre: ");
        char nombre[50];
    scanf("%s",&nombre);
     fprintf(clientes, "Nombre: %s\n", nombre);
     fclose(clientes);
 /////////////////////////separador
     
         FILE *Productos = fopen("Productos.txt", "a");
        printf("Producto: \n");
        char Mercancia[50];
    scanf("%s",&Mercancia);
     fprintf(Productos, "Productos: %s\n", Mercancia);
     fclose(Productos);
    /////////////////////////separador
     
              FILE *Fechas = fopen("Fechas.txt", "a");
        printf("Fecha: \n");
        int Day;
    scanf("%d",&Day);
     fprintf(Fechas, "Fechas: %d\n", Day);
     fclose(Fechas);
    
    
}
void copiarContenido(FILE *origen, FILE *destino) {
    char linea[100];
    while (fgets(linea, sizeof(linea), origen)) {
        fprintf(destino, "%s", linea);
    }
}
